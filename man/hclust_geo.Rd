% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hclust_geo.R
\name{hclust_geo}
\alias{hclust_geo}
\title{Cluster points using standard hierarchical clustering on geographic
distance only (no depth, no spatial contiguity constraint).}
\usage{
hclust_geo(
  points,
  x_col = "X_standard",
  y_col = "Y_standard",
  id_col = "UNIQUE_ID",
  habitat_col = "habitat",
  distance_method = "euclidean",
  n_points = 204,
  n_clust = round(nrow(points)/n_points),
  method = "ward.D2",
  interpolation_threshold = 30000
)
}
\arguments{
\item{points}{sf data.frame. Contains geometries and \code{x_col}/\code{y_col}
coordinate columns.}

\item{x_col}{character. Name of the column holding X coordinates. Default = "X_standard".}

\item{y_col}{character. Name of the column holding Y coordinates. Default = "Y_standard".}

\item{id_col}{character. Column holding ID value for the target reef
(attached to the site_id values on output). Default = "UNIQUE_ID".}

\item{habitat_col}{character. Column holding unique habitat values
(attached to \code{id_col} value and site_id values on output). Default = "habitat".}

\item{distance_method}{character. Distance matrix creation method for the
geographic coordinates. Default = "euclidean" (see \code{dist()}).}

\item{n_points}{integer numeric. Desired number of points per cluster.
Used to calculate \code{n_clust} (number of output clusters). Default = 204.}

\item{n_clust}{integer numeric. Number of clusters in result output.
(Point to cut hierarchical clustering tree). Default =
\code{round(nrow(points) / n_points)}.}

\item{method}{character. Agglomeration method passed to \code{stats::hclust()}.
Default = "ward.D2".}

\item{interpolation_threshold}{numeric. Habitats with more points than
this are subsampled to \code{interpolation_threshold} points for clustering,
then remaining points are assigned via nearest-neighbour interpolation -
same handling \code{constrained_hclust_mst()} uses for large habitats.
Default = 30000.}
}
\value{
data.frame of points with allocated site_ids based on cluster
outputs. \code{site_id} values are a combination of the \code{id_col} value,
\code{habitat_col} value and the cluster allocation.
}
\description{
Take a dataframe of points and cluster them using
\code{stats::hclust()} on a geographic (X/Y) distance matrix only - no depth
or other additional variable contributes to the distance matrix, and no
minimum spanning tree is built. Unlike \code{constrained_hclust()} /
\code{constrained_hclust_mst()}, no \code{links}/contiguity graph is used either:
this is plain, unconstrained agglomerative clustering, so at each merge
step any two clusters can be joined purely because they are close in
X/Y distance, regardless of what (if anything) lies between them.
Resulting sites are therefore not guaranteed to be spatially contiguous,
unlike every other clustering function in this package.
}
