# Internal helper function that fills NA values from columns with values from the nearest neighbouring points.

Internal helper function that fills NA values from columns with values
from the nearest neighbouring points.

## Usage

``` r
fill_na_nearest(pixel_data, columns)
```
