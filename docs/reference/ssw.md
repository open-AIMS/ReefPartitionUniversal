# Internal helper to compute the sum of square distances within cluster nodes. Follows spdep function, implementing igraph methods.

Internal helper to compute the sum of square distances within cluster
nodes. Follows spdep function, implementing igraph methods.

## Usage

``` r
ssw(data, nodes, method = "euclidean", p = 2, cov = NULL, inverted = FALSE)
```
